<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="KiviPaperiSakset.Views.MainMenuView"
             Background="#2c3e50">
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="20">
        <TextBlock Text="🎮 Kivi–Paperi–Sakset" 
                   FontSize="32" 
                   Foreground="White" 
                   TextAlignment="Center"
                   FontWeight="Bold"
                   Margin="0,0,0,30"/>
        
        <Button Command="{Binding StartGameCommand}"
                Content="Aloita peli"
                Padding="20,15"
                FontSize="18"
                Width="200"
                Background="#27ae60"
                Foreground="White"
                FontWeight="Bold"
                CornerRadius="5"/>
        
        <Button Command="{Binding ExitCommand}"
                Content="Lopeta peli"
                Padding="20,15"
                FontSize="18"
                Width="200"
                Background="#e74c3c"
                Foreground="White"
                FontWeight="Bold"
                CornerRadius="5"/>
    </StackPanel>
</UserControl>
